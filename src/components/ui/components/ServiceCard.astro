---
import { Icon } from "astro-icon/components";
interface Props {
    iconClass?: string;
    title?: string;
    description?: string;
    extraClasses?: string;
}
const {
    iconClass = "mdi:mdi:rocket-launch",
    title = "Innovación constante",
    description = "Impulsamos la innovación en tu empresa con estrategias efectivas y personalizadas.",
    extraClasses = ""
} = Astro.props as Props;
---

<div class={`bg-yellow-400 p-4 rounded-xl font-[700] h-[500px] flex flex-col items-start justify-center gap-5 ${extraClasses}`}>
  <div class="flex flex-col items-start gap-2">
    <div class="bg-black text-white p-2 rounded-full text-2xl">
      <Icon name={iconClass} />
    </div>
    <span class="text-2xl text-start">{title}</span>
  </div>
  <p class="text-start">{description}</p>
</div>