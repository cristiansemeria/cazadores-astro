---
interface Props {
    title?: string;
    subtitle?: string;
    extraClasses?: string;
}

const {
    title = "Título por defecto",
    subtitle = "",
    extraClasses = "",
} = Astro.props as Props;
---

<div class={`container mx-auto text-center my-10 font-bold ${extraClasses}`}>
    <span class="text-lg sm:text-xl">{subtitle}</span>
    <div class="flex justify-center items-center">
        <h2 class="text-4xl sm:text-6xl lg:w-1/2 text-center">{title}</h2>
    </div>
    <slot />
</div>